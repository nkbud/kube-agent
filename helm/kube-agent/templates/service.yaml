apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-agent.fullname" . }}
  labels:
    {{- include "kube-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kube-agent.selectorLabels" . | nindent 4 }}